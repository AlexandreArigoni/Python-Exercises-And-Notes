"""Nested (Aninhadas, one inside other) Conditions"""
if, elif, else -> Used for that
if x:      #right
    ...
    
elif:      #go ahead   (else + if) -> U can use how many elifs u want
    ...
    
else:      #left
    ...

*in = inside
